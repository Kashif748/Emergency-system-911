import { PropTranslatorPipe } from './prop-translator.pipe';

describe('PropTranslatorPipe', () => {
  it('create an instance', () => {
    const pipe = new PropTranslatorPipe();
    expect(pipe).toBeTruthy();
  });
});
