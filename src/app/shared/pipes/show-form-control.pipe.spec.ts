import { ShowFormControlPipe } from './show-form-control.pipe';

describe('ShowFormControlPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowFormControlPipe();
    expect(pipe).toBeTruthy();
  });
});
