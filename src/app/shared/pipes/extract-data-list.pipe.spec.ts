import { ExtractDataListPipe } from './extract-data-list.pipe';

describe('ExtractDataListPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractDataListPipe();
    expect(pipe).toBeTruthy();
  });
});
