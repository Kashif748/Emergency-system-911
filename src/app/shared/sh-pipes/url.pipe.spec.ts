/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { UrlPipe } from './url.pipe';

describe('Pipe: url', () => {
  it('create an instance', () => {
    let pipe = new UrlPipe(null, null, null);
    expect(pipe).toBeTruthy();
  });
});
