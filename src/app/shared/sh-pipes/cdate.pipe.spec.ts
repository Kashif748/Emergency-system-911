/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CdatePipe } from './cdate.pipe';

describe('Pipe: Cdatee', () => {
  it('create an instance', () => {
    let pipe = new CdatePipe();
    expect(pipe).toBeTruthy();
  });
});
